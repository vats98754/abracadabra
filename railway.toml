[build]
builder = "NIXPACKS"

[deploy]
startCommand = "bash start_webhook.sh"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[nixpacks]
pythonVersion = "3.11"
